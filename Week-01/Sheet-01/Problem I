#include <iostream>

int main(){
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(NULL);

    double x{}, y{};
    std::cin >> x >> y;
    
    if(x >= y) std::cout << "Yes" << '\n';
    else std::cout << "No" << '\n';
}
