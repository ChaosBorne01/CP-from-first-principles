#include <iostream>

int main(){
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(NULL);

    long long int x{}, y{};
    std::cin >> x >> y;
    
    if(x%y == 0 || y%x == 0) std::cout << "Multiples" << '\n';
    else std::cout << "No Multiples" << '\n';
}
