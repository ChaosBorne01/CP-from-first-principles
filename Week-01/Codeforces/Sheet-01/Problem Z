#include <bits/stdc++.h>
using std::cout;
using std::cin;
using std::vector;
using std::pair;
using std::string;
using std::sort;

int main(){
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(NULL);

    long long A{}, B{}, C{}, D{};
    cin >> A >> B >> C >> D;

    long double x = std::log10l(A);
    long double y = std::log10l(C);

    x *= B;
    y *= D;

    if(x > y) cout << "YES\n";
    else cout << "NO\n";
}
